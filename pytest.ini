[pytest]
# AGENTS.md: never write to /tmp; keep pytest's tmp_path under the project
addopts = --basetemp=tests/.pytest_tmp
# tests/tmp may be left owned by root from a privileged run; dont let an
# unreadable leftover break collection
norecursedirs = tmp tmp_* tests/tmp
